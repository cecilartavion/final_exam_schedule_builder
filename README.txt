To get strip_website.py working so that it pulls the class data, put the latest version of chromedriver in the C://Windows folder.

Next, run strip_website.py with the proper variable name for the semester and year. 

Then run get_courses_new.py to have the cliques come out. 